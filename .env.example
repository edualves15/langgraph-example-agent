APP_ENV=local

# Gemini (obrigatório)
GEMINI_API_KEY=change-me
GEMINI_MODEL=gemini-3.1-flash-lite

# Tavily (opcional) — habilita as tools web_search / web_extract
TAVILY_API_KEY=change-me

# Mantida por compatibilidade; atualmente não usada pelo create_react_agent.
MAX_TOOL_CALLS=10
